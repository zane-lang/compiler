; Stage 3: semantics, which builds the typed syntax tree. See
; docs/semantics.md. Today it holds only the assembly of packages from
; directories, the first step of that document's build order.
(library
 (name tst)
 (libraries cst diagnostic sst source tree_graph))
