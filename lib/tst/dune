; Stage 3: semantics, which builds the typed syntax tree. See
; docs/semantics.md: [Assembly] groups files into packages, [Collect],
; [Types], [Signatures] and [Check] are the five passes, [Read_only],
; [Guests] and [Moves] are analyses over the tree they build, and
; [Semantics] runs them in order.
(library
 (name tst)
 (libraries cst diagnostic sst source tree_graph))
