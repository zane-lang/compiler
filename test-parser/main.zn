main (this Int, a Bool)! Void?String {
	x [this Int]! -> Void = "hello"
	y String = "world"
	content String = read("wordlist.txt") ?? ""
	results List = crawl(content) error ? {
		print(error)
		abort error
	}
}
