/// A `String`-speaking wrapper over the runtime's console (effects.md §6.6).
package core;

type Console = #struct {
	_console &@runtime$Console;
}

Console(console &@runtime$Console) => init{ _console = console; }

Unit print(this Console, text String) mut {
	this._console!print(text.raw);
	return Unit();
}
