/// `print` takes a guest, and a guest names a place: a view built in the
/// argument is a temporary, so it is stored first (memory.md §2.8).
package temporary;

@primitives$Unit main() {
	@program$console!print(@primitives$String("hello world"));
	return @primitives$Unit();
}
