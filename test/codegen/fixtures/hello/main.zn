/// The smallest program that runs: a string view stored from a literal
/// (types.md §2.7), and a guest to it handed to the runtime's console.
package hello;

@primitives$Unit main() {
	text @primitives$String("hello world");
	@program$console!print(text);
	return @primitives$Unit();
}
